# SocketIO client in PHP

Author: Mark Karpeles

See example.php for an example usage of this.

We are using PHP 5.3 syntax, so this code is not compatible with earlier PHP version (backporting would be trivial, but I don't feel like it).
